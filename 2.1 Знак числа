#In mathematics the function sign(x) (number sign) are defined as follows:
#sign(x) = 1 if x > 0,
#sign(x) = -1 if x < 0,
#sign(x) = 0 if x = 0.
#For a given number x, output the value sign(x).
print('Enter enter the value of x')
x=float(input()) #given the value of x
if x>0: 
    print('1')
elif x<0:
    print('-1')
else:
    print('0')
